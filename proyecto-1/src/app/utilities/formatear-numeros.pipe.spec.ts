import { FormatearNumerosPipe } from './formatear-numeros.pipe';

describe('FormatearNumerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearNumerosPipe();
    expect(pipe).toBeTruthy();
  });
});
